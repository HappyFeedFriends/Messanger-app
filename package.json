{
  "name": "website",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "server": "npm start --prefix back",
    "client": "npm start --prefix front"
  },
  "dependencies": {
    "socket.io-client": "^2.3.0"
  }
}
